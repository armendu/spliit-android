<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug-build only (see app/src/debug/AndroidManifest.xml) — this file is never merged into a
  release build, so production traffic stays HTTPS-only under the platform default.

  Without this, pointing the app at a self-hosted instance served over plain HTTP — which is
  exactly what testing against `make e2e-up`'s Docker instance from the emulator requires, per
  CLAUDE.md's `10.0.2.2` note — fails with "CLEARTEXT communication ... not permitted by network
  security policy" the moment a real request is made, rather than at build time. `10.0.2.2` is the
  emulator's address for the host loopback; `localhost`/`127.0.0.1` are included for a device or
  a differently-configured emulator reaching a local instance directly.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
    </domain-config>
</network-security-config>
